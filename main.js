
$('.alltheitems').html('Hello World')

/*
function boardTemplate (board) {
  var spacesHtml = board.map(function (x) { return `<div class="space">${x}</div>`; });
  return `
    <div class="board">
      ${spacesHtml.join("\n")}
    </div>
  `;
}
*/
